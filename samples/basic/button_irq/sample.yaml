sample:
  name: Button(IRQ) Sample
tests:
  sample.basic.button_irq:
    tags:
      - button
      - gpio
    filter: dt_enabled_alias_with_parent_compat("sw0", "gpio-keys")
    integration_platforms:
      - rbs2530_cpu
    depends_on: gpio
    harness: button
