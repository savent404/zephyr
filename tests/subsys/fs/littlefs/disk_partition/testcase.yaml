common:
  tags:
    - filesystem
    - littlefs
  modules:
    - littlefs
tests:
  filesystem.littlefs.disk_partition:
    platform_allow:
      - qemu_x86_64
    integration_platforms:
      - qemu_x86_64
    timeout: 120
